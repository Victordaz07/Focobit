{
  "name": "focobit",
  "private": true,
  "scripts": {
    "mobile": "pnpm --filter mobile start",
    "web": "pnpm --filter web dev",
    "functions": "pnpm --filter functions dev",
    "build:web": "pnpm --filter web build",
    "build:functions": "pnpm --filter functions build",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
